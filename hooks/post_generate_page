#!/bin/bash

set -e

#if [ "$PAGEGEN_PAGE_TITLE" = "Download" ]; then
#  echo "Gotcha!"
#fi

# Compress file
if [ "$PAGEGEN_TARGET_ENVIRONMENT" != "test" ]; then
  htmlcompress --type html -o "$PAGEGEN_PAGE_TARGET_PATH" "$PAGEGEN_PAGE_TARGET_PATH"
fi